# Self-Healing-Distributed-Object-Store
The project builds a fault-tolerant distributed object storage system that preserves data integrity even when stored on untrusted/failing servers. By combining Erasure Coding with Cryptographic Hashing (for integrity), the system can detect corrupted data and automatically heal itself by reconstructing lost or altered fragments.
