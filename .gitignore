# ── Maven build output ────────────────────────────────────────────────────────
target/

# ── Runtime cluster data (logs + shards written during a run) ─────────────────
cluster-data/

# ── PoR run outputs (generated fresh on every upload) ─────────────────────────
*.manifest
*.porkey

# ── Downloaded / recovered output files ───────────────────────────────────────
recovered*.txt
recovered*.bin
*.recovered

# ── Temporary / scratch node data directories ─────────────────────────────────
tmp-*/
demo-node-data/
data/

# ── macOS noise ───────────────────────────────────────────────────────────────
.DS_Store

# ── Agent/AI instruction files (local-only, never pushed) ─────────────────────
AGENTS.md
CLAUDE.md
